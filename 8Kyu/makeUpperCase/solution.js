/*
    MakeUpperCase
    
    Write a function which converts the input string 
    to uppercase.
 */

// P.aramters R.eturns E.xamples P.suedo code
// single string input
// single string uppercase
// "art" -> "ART"
function makeUpperCase(str) {
    return str.toUpperCase();
}

module.exports = makeUpperCase;